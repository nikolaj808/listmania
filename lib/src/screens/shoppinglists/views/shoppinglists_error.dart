import 'package:flutter/material.dart';

class ShoppinglistsError extends StatelessWidget {
  const ShoppinglistsError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('An error occurred'),
    );
  }
}

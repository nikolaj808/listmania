import 'package:flutter/material.dart';

class ShoppinglistsEmpty extends StatelessWidget {
  const ShoppinglistsEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('No shoppinglists'),
    );
  }
}

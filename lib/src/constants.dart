import 'package:flutter/material.dart';

// TODO: Fjern når du har besluttet dig
// Denne kunne bruges som accent color??
const Color kAccentColor = Color(0xFFE25241);
const Color kBackgroundColor = Color(0xFF252525);
const Color kListTileColor = Color(0xFF3D3D3D);
const double kDefaultVerticalPadding = 16.0;
const double kDefaultHorizontalPadding = 32.0;
